[project]
name = "url-shortener"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "pydantic>=2.6",
]

[project.optional-dependencies]
test = [
  "pytest>=8.0",
  "httpx>=0.26",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
